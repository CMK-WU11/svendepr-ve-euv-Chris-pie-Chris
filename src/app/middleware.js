// taget fra gennemgang af terminsopgaven
// import { NextResponse } from "next/server";

// export function middleware(request) {
//     const token = request.cookies.get("dance_token")
//     const uid = request.cookies.get("dance_uid")

//     if (!(token && uid)) {
//         return NextResponse.redirect(new URL("/", request.url))
//     }
// }

// export const config = {
//     matcher: '/Kalender/:path*'
// }